name: leyendo otro pipeline

on:
  push:
    branches:
      - main
jobs:
  nuevo-build:
    runs-on: ubuntu-latest

    steps:
      - name: Se clona el codigo de mi repo
        uses: actions/checkout@v2

      - name: Ejecucion del pipeline del repo source
        run: |
          cd .github/workflows/laboratorio-workflow2
          chmod +x pipeline.yml
          .pipeline.yml

